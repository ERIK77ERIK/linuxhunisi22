#include <stdio.h>
#include <math.h>

int main()
{
// index tas ete 1 a sarqi 0 ete 0a toxi nuyny

/*
        printf("write number - \n");
        int num;
        scanf("%d", &num);

        printf("write index1  - \n");
        int index1;
        scanf("%d", &index1);

        printf("write index2  - \n");
        int index2;
        scanf("%d", &index2);

        int x = ~(1 << index1);
        int z = (num & x);
        int y = z | (1 << index1);
        printf("%d", y);  // (num & ~(1 << index1)) | 1 << index1;




// index tas ete 0 dardsni 1 ete 1a toxi nuyny
        int a = 1 << index2;
        int b = num | a;

        printf("result - %d", b);
// tivy ete kam enq anum 1-i het, 0-n dardsnuma 1, 1-y toxuma nuyny  (num | 1<<index),  isk kam aneluc 0-i het ban chi linum
// tivy ete ev enq anum 0-i het, 1-y dardsnuma 0, 0-n toxuma nuyny  (num & ~(1<<index)), isk ev aneluc 1-i het ban chi linum

*/
        
}

